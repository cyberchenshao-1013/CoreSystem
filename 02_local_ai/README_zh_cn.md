# 02_local_ai

`02_local_ai/` 是本地小AI模型与推理运行时目录，专注于端侧离线 AI 能力和隐私处理。

## 结构

- `01_models/`：存放本地小模型权重，如 GGUF/AWQ 格式的轻量模型文件。
- `02_runtimes/`：本地推理引擎与运行环境，如 llama.cpp、Ollama、Executorch 等。
- `03_scripts/`：用于加载、压测模型或以 API 方式挂载本地模型的脚本。

## 目标

打造一个轻量级、本地可运行的 AI 平台，用于日常离线推理、自动化代理以及私密文本和数据处理。
