# 01_bin

`01_bin/` 是系统级命令与可执行脚本目录，主要面向命令行驱动的自动化控制。

## 结构

- `01_sys_control/`：底层系统控制脚本，如电源、休眠、锁屏、截屏、网络代理开关等。
- `02_cli_tools/`：自定义 CLI 工具，用于文件同步、跨设备剪贴板、快速操作和系统管理。
- `03_hooks/`：系统级钩子，用于在执行工具前后执行校验、自动化检查或安全策略。

## 目标

构建一个统一的可执行入口，使本地系统操作与自动化任务能够通过命令行快速调用、组合和扩展。
